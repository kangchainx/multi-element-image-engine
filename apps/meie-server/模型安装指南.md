# ComfyUI 模型安装指南

## ❗ 当前问题

你的workflow需要模型 `v1-5-pruned-emaonly-fp16.safetensors`，但你的ComfyUI中还没有安装任何模型。

## 📥 如何安装模型

### 方法1: 使用ComfyUI界面安装（推荐）

1. 打开ComfyUI: http://127.0.0.1:8000
2. 点击右侧的 "Manager" 按钮
3. 选择 "Model Manager"
4. 搜索 "Stable Diffusion v1.5"
5. 点击下载

### 方法2: 手动下载模型

#### 推荐的入门模型：

**Stable Diffusion v1.5** (最常用的基础模型)
- 下载地址: https://huggingface.co/runwayml/stable-diffusion-v1-5/blob/main/v1-5-pruned-emaonly.safetensors
- 文件大小: ~4GB

**更小的测试模型：**
- Stable Diffusion v1.4: https://huggingface.co/CompVis/stable-diffusion-v1-4

#### 下载后放到模型目录：

根据你的ComfyUI配置，模型应该放在：
```
/Users/chris/Documents/ComfyUI/models/checkpoints/
```

如果这个文件夹不存在，创建它：
```bash
mkdir -p /Users/chris/Documents/ComfyUI/models/checkpoints
```

### 方法3: 检查是否已有模型但未识别

运行这个命令查看你的模型目录：
```bash
ls -la /Users/chris/Documents/ComfyUI/models/checkpoints/
```

## 🔄 下载模型后

1. 重启ComfyUI（如果需要）
2. 检查模型是否被识别：
   - 打开 http://127.0.0.1:8000
   - 添加一个 "Load Checkpoint" 节点
   - 看下拉菜单中是否有模型

3. 更新workflow文件中的模型名称

## 🧪 验证模型安装

运行这个命令检查ComfyUI是否识别到模型：

```bash
curl -s http://127.0.0.1:8000/object_info/CheckpointLoaderSimple | grep -o '"ckpt_name":\s*\[\[[^]]*\]\]'
```

如果显示模型列表，说明安装成功！

## 🚀 安装完成后

1. 在ComfyUI界面中导出新的workflow（使用实际存在的模型）
2. 保存为 `Unsaved Workflow.json`
3. 运行 `npm start` 测试

## 📋 其他推荐模型

安装好基础模型后，你可以尝试：

- **Realistic Vision**: 更真实的图片风格
- **Anything V5**: 动漫风格
- **Dreamshaper**: 艺术风格

所有模型都放在 `/Users/chris/Documents/ComfyUI/models/checkpoints/` 目录下。
